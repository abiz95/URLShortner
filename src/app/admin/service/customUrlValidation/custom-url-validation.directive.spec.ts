import { CustomUrlValidationDirective } from './custom-url-validation.directive';

describe('CustomUrlValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomUrlValidationDirective();
    expect(directive).toBeTruthy();
  });
});
